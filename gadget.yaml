name: ptrace
description: tracing ptrace system calls
homepageURL: 'TODO: Fill the gadget homepage URL'
documentationURL: 'TODO: Fill the gadget documentation URL'
sourceURL: 'TODO: Fill the gadget source code URL'
datasources:
  ptrace:
    fields:
      comm:
        annotations:
          description: 'TODO: Fill field description'
      mntns_id:
        annotations:
          description: 'TODO: Fill field description'
      pid:
        annotations:
          description: 'TODO: Fill field description'
      request_name:
        annotations:
          description: ptrace request type
      target_pid:
        annotations:
          description: process id of the target process
      timestamp:
        annotations:
          description: 'TODO: Fill field description'
